import { TaskController } from "./taskController.js";

// HTMLが読み込まれたらアプリを開始
document.addEventListener('DOMContentLoaded', () => {
    // TaskControllerのインスタンスを作成
    const app = new TaskController();
})
